services:
  postgres:
    image: postgres:16
    container_name: padel-db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: padel_connect
    ports:
      - "5432:5432"